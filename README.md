In order to this application to work you'll need "matplotlib", a library used for creating graphs, such as pie graphs.

to install it, just run this command on your CMD:

python -m install matplotlib
